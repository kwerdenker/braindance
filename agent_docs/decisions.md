# Decision log

Append-only. One line per answered question, so a decision made once is not re-opened.
Read this before asking anything that sounds settled.

2026-07-31 — Q: Amend `50b8d13` with step 7's review fixes, or commit on top? → Decision: commit on top. Amending orphans the base `own-libfreenect2` is built on, so the merge would carry two commits both claiming to be step 7, one with a footage-loss regression.
2026-07-31 — Q: Merge, rebase or cherry-pick `own-libfreenect2`? → Decision: merge. Shared base and a one-file conflict make it cheap either way; the commit bodies are this repo's documentation, and the branch is checked out in a live worktree so rebasing it in place is out.
2026-07-31 — Q: Resolve the `CLAUDE.md` conflict by taking a side? → Decision: no, union. The two sides describe different things and neither supersedes the other.
2026-07-31 — Q: Should the route sweep assert the open take's size during the drive as well as after close? → Decision: after close only. The during-drive delta reads 0 on 40 of 40 samples but that is a syscall-width window, not a guarantee; one exact row beats two where the extra can lie.
2026-07-31 — Q: Close the query-parameter plant shape? → Decision: no, record it as a known hole. No enumeration of the route table can reach a parameter the table does not declare; a lint over `read` handlers touching `query` is a different tool.
2026-07-31 — Q: Fix `vendor-check` proving the source rather than the built artifact before merging? → Decision: no, land it and record the gap in the merge commit body. It is a coverage gap, not a broken product.
2026-07-31 — Q: Does the open-take sweep blindness block the step-7 commit? → Decision: yes, fixed before committing. The reviewer judged it non-blocking since nothing does that shape today, but the sweep exists to catch a route added later and step 8 adds routes.
2026-07-31 — Q: Change the registration thread count from 2? → Decision: no, not without measuring on the capture node. The Mac's answer was 4 and 4 was the worst setting there.
2026-07-31 — Q: Re-run all 87 mutations independently after round 5, or scope it? → Decision: scope to the 46 library mutations plus the 8 clean runs. Only `server/library.js` and `tools/library-check.mjs` changed; the other tools exercise unchanged `web/main.js` and were already green on this code.
2026-07-31 — Q: Where does the WebSocket `Origin` check live? → Decision: its own commit, after step 7. All five reserved regions in `server/index.js` are byte-identical to `50b8d13` so that commit can own them cleanly.
2026-07-31 — Q: Did the pre-planned `CLAUDE.md` conflict need hand-resolving? → Decision: no, git produced the union itself; verified by diffing the result against both parents and requiring zero removed lines from either, rather than by reading it. The plan's three corrections beyond the union were applied by hand.
2026-07-31 — Q: Trust `vendor-check` alone that the rebuild happened? → Decision: no, probe the artifact. `nm -gU | grep -ci thread`, imported pthread symbols and `std::thread` machinery all read the same on a stale library and a threaded one, because libfreenect2 already threads its packet processors. The discriminating probe is the string `LIBFREENECT2_REG_THREADS`, absent from the Jul-30 prefix and present after the rebuild.
2026-07-31 — Q: Is `registration-check`'s default run enough? → Decision: no, run `--persistent-subject` too. The default compares `apply()`'s four-argument form both sides, but `grabber.cpp` ships the seven-argument one, so the documented invocation proves a call shape the product does not use. Both arms are bit-identical on the 72-frame corpus.
2026-07-31 — Q: Preserve the stale `vendor/prefix` before rebuilding? → Decision: yes. It is exactly the "prefix built from different source" that `vendor-check`'s recorded gap needs as a falsification control, and the rebuild would have destroyed it. Kept as `prefix-stale-jul30` in the session scratchpad.
2026-07-31 — Q: Which mutation convention should `guard-check` follow? → Decision: the four server tools', not the two vendoring ones'. Caught exits non-zero, the refusal message reads `unknown mutation X - have a, b`, and `FAIL` carries two leading spaces, so `sweep-all.mjs` can enumerate and count it. The two vendoring tools are not sweepable on any of those three points and that is a known divergence, not a thing to spread.
2026-07-31 — Q: Does the WebSocket origin check make `--host` unnecessary? → Decision: no, they are different failures. Host equality cannot survive DNS rebinding — a name the attacker controls resolving to the node's LAN address makes `Origin` and `Host` genuinely equal — so the guard stops the drive-by and the loopback default stops the thing that has to be routable to be attacked at all.
2026-07-31 — Q: Start implementing step 8 in this session? → Decision: no, write the brief and hand it over. Step 8 is a whole build-order step and half a job queue is worse than none; the brief carries the two things the lost one had to carry — the namespace seam derived from `ROUTES` rather than the hardcoded five, and `server/export.js` already writing project/capture/renderer.
2026-07-31 — Q: Close `vendor-check`'s artifact gap after all? → Decision: yes, partly, and say which part. Section 5 asserts the installed library carries `LIBFREENECT2_REG_THREADS`, with `--mutate stale-prefix` pointing at `vendor/prefix-oracle` as the control; the sub-9 fix stays source-only because `& 0x1ff` leaves nothing in the binary. Demonstrated against the real Jul-30 stale prefix, which now fails at exit 1.
2026-08-01 — Q: Does an unowned namespace let `/jobs/../web/main.js` traverse, as the step-8 handoff said? → Decision: no, the claim is false and was measured. `new URL()` removes dot segments including `%2e%2e`, and `isInside` rejects what is left; four escape attempts served nothing. The real property is shadowing — a file under an unowned namespace IS served (200 with contents), the same file under an owned one is the API's 404 — and that is what the seam and its check are about.
2026-08-01 — Q: Is a one-segment probe enough for the namespace-shadowing row? → Decision: no, it sat in a dead zone. `/presets/shadow-probe.js` is claimed by `/presets/:name`'s `([^/]+)`, so the table answered and the fallthrough was never reached; the control ran the whole suite and was NOT caught at 255 assertions, none failed. The probe is two segments deep now, with an unowned-namespace twin asserting the file really is servable.
